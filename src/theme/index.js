import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      light: '#9cfce4',
      main: '#8ea95b',
      dark: '#18573c',
    },
    background: {
      default: '#d3d3d3',
    },
  },
});

export default theme;
